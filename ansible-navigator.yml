---
ansible-navigator:
  execution-environment:
    image: hub01.jferre.local/ee-mysql-custom:latest # WITH MYSQL COLLECTION
    pull:
    #  arguments:
    #    - "--tls-verify=false"
      policy: missing
  playbook-artifact:
    enable: false
